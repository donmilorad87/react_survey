
import ReactDOM from 'react-dom/client';

import App from './App';

// Initialize the app
const rootElement = document.getElementById('ccTestDialog');
const root = ReactDOM.createRoot(rootElement!);
root.render(<App />);